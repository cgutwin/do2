package ca.cgutwin.deckedout.systems.clank.events;

import ca.cgutwin.deckedout.events.Event;

public class ClankIncreaseEvent extends Event {
  public ClankIncreaseEvent(String type, String data) {
    super(type, data);
  }
}

package ca.cgutwin.deckedout.systems.clank.events;

import ca.cgutwin.deckedout.events.Event;

public class MaxClankReachedEvent extends Event {
  public MaxClankReachedEvent(String type, String data) {
    super(type, data);
  }
}

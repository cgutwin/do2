package ca.cgutwin.deckedout.systems.hazard.events;

import ca.cgutwin.deckedout.events.Event;

public class MaxHazardReachedEvent extends Event {
  public MaxHazardReachedEvent(String type, String data) {
    super(type, data);
  }
}

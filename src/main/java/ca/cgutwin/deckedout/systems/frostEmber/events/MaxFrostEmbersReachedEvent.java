package ca.cgutwin.deckedout.systems.frostEmber.events;

import ca.cgutwin.deckedout.events.Event;

public class MaxFrostEmbersReachedEvent extends Event {
  public MaxFrostEmbersReachedEvent(String type, String data) {
    super(type, data);
  }
}
